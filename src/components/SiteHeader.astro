---
const navItems = [
  { href: '/', label: 'Home' },
  { href: '/blog/', label: 'Blog' },
  { href: '/about/', label: 'About the Lab' },
];
---

<header class="shell">
  <div class="brand">
    <div class="mark" aria-hidden="true">
      <span class="dot dot--primary" />
      <span class="dot dot--secondary" />
    </div>
    <div class="copy">
      <a href="/" class="title">Astro Minimal JS Lab</a>
      <p class="tagline">Static-first, content-heavy, almost zero JavaScript.</p>
    </div>
  </div>

  <nav aria-label="Primary navigation">
    <ul class="nav-list">
      {navItems.map((item) => (
        <li>
          <a href={item.href}>{item.label}</a>
        </li>
      ))}
    </ul>
  </nav>
</header>

<style>
  .shell {
    position: sticky;
    top: 0;
    z-index: 20;
    width: 100%;
    max-width: 1120px;
    margin: 1.5rem auto 0;
    padding: 0.85rem 1.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    border-radius: 999px;
    border: 1px solid rgba(148, 163, 184, 0.5);
    background: linear-gradient(120deg, rgba(15, 23, 42, 0.9), rgba(15, 23, 42, 0.95));
    backdrop-filter: blur(22px);
    box-shadow: 0 18px 40px rgba(15, 23, 42, 0.85);
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 0.85rem;
  }

  .mark {
    position: relative;
    width: 32px;
    height: 32px;
    border-radius: 999px;
    background: radial-gradient(circle at 30% 20%, #f97316, #1f2937);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow:
      0 0 0 1px rgba(15, 23, 42, 0.9),
      0 0 40px rgba(248, 250, 252, 0.22);
    overflow: hidden;
  }

  .dot {
    position: absolute;
    border-radius: 999px;
    opacity: 0.85;
  }

  .dot--primary {
    width: 22px;
    height: 22px;
    background: radial-gradient(circle at 20% 20%, #f97316, #fbbf24);
  }

  .dot--secondary {
    width: 38px;
    height: 38px;
    background: radial-gradient(circle at 60% 80%, rgba(56, 189, 248, 0.7), transparent);
    mix-blend-mode: screen;
  }

  .copy {
    display: flex;
    flex-direction: column;
    gap: 0.1rem;
  }

  .title {
    font-weight: 600;
    font-size: 0.98rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .tagline {
    margin: 0;
    font-size: 0.72rem;
    color: #9ca3af;
  }

  nav {
    flex-shrink: 0;
  }

  .nav-list {
    display: flex;
    align-items: center;
    gap: 1.3rem;
    list-style: none;
    margin: 0;
    padding: 0;
    font-size: 0.84rem;
  }

  .nav-list a {
    color: #e5e7eb;
    text-decoration: none;
    position: relative;
    padding-block: 0.15rem;
  }

  .nav-list a::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 1px;
    transform-origin: left;
    transform: scaleX(0);
    background: linear-gradient(90deg, #22d3ee, #f97316);
    transition: transform 140ms ease-out;
  }

  .nav-list a:hover::after {
    transform: scaleX(1);
  }

  @media (max-width: 720px) {
    .shell {
      flex-direction: column;
      align-items: flex-start;
      border-radius: 20px;
    }

    nav {
      width: 100%;
    }

    .nav-list {
      justify-content: space-between;
    }
  }
</style>


